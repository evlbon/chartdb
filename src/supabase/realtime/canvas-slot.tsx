import type React from 'react';

// Заглушка: реализуется на этапе 5 (живые курсоры поверх канвы).
// Рендерится внутри <ReactFlow> — можно использовать ViewportPortal.
export const SupabaseCanvasSlot: React.FC = () => null;
